<UserControl x:Class="AutoTrackApp.UI.Components.Icons.CheckIcon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="root"
             Width="{Binding Size, RelativeSource={RelativeSource Self}}"
             Height="{Binding Size, RelativeSource={RelativeSource Self}}">
    <Viewbox Stretch="Uniform">
        <Canvas Width="24" Height="24">
            <Path Data="M20.285,6.709c0.391,0.391,0.391,1.023,0,1.414l-11,11c-0.391,0.391-1.023,0.391-1.414,0l-5-5c-0.391-0.391-0.391-1.023,0-1.414s1.023-0.391,1.414,0L9,16.586l10.578-10.577C19.262,6.316,19.895,6.316,20.285,6.709z">
                <Path.Fill>
                    <MultiBinding Converter="{StaticResource IsMouseOverToBrushConverter}">
                        <Binding ElementName="root" Path="IsMouseOver"/>
                        <Binding ElementName="root" Path="IconFill"/>
                        <Binding ElementName="root" Path="HoverFill"/>
                    </MultiBinding>
                </Path.Fill>
            </Path>
        </Canvas>
    </Viewbox>
    <UserControl.Style>
        <Style TargetType="UserControl">
            <Setter Property="Cursor" Value="Arrow"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>
</UserControl>
