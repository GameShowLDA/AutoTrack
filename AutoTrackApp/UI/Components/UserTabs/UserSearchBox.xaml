<UserControl x:Class="AutoTrackApp.UI.Components.UserTabs.UserSearchBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="36" MinWidth="200">
    <Grid>
        <Border Background="White"
                BorderBrush="#e0e0e0"
                BorderThickness="1"
                CornerRadius="6">
            <TextBox x:Name="SearchTextBox"
                     Margin="0"
                     Padding="10,0"
                     Height="36"
                     VerticalContentAlignment="Center"
                     FontSize="16"
                     Background="Transparent"
                     BorderThickness="0"
                     Text="{Binding SearchText, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}"/>
        </Border>
        <TextBlock Text="Поиск..."
                   Foreground="#888"
                   FontSize="16"
                   Padding="20,0,0,0"
                   VerticalAlignment="Center"
                   IsHitTestVisible="False"
                   Visibility="{Binding Text, ElementName=SearchTextBox, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}"/>
    </Grid>
</UserControl>